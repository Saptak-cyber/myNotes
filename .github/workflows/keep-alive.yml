name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */2 * * *"   # runs every 2 hours
  workflow_dispatch:         # allows manual run from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase URL
        run: curl -I "https://pyoouvmcamkyvfvefpjb.supabase.co"
